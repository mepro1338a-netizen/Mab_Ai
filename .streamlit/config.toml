[client]
showSidebarNavigation = false

[server]
headless = true
address = "0.0.0.0"
# Port: set via Railway $PORT in start.sh — do not hardcode here.
# Railway reverse proxy: XSRF with wrong origin (0.0.0.0) breaks the UI — keep off in prod.
enableCORS = false
enableXsrfProtection = false
enableWebsocketCompression = false
# Keepalive for Railway proxy (Streamlit 1.48+); lowers idle-timeout disconnects.
websocketPingInterval = 25
maxUploadSize = 200

[browser]
gatherUsageStats = false

[runner]
fastReruns = true

[logger]
level = "info"

[theme]
base = "dark"
primaryColor = "#7c3aed"
backgroundColor = "#09090b"
secondaryBackgroundColor = "#18181b"
textColor = "#fafafa"

[theme.sidebar]
backgroundColor = "#18181b"
secondaryBackgroundColor = "#27272a"
textColor = "#e4e4e7"
